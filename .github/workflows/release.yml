name: Update Version and Tag
on:
  push:
    branches:
      - master

permissions:
  contents: write
  actions: write

jobs:
  release:
    uses: peavers-warcraft/workflow-templates/.github/workflows/release-on-push.yml@main
    with:
      toc_file: PeaversAlwaysSquare.toc
      addon_name: PeaversAlwaysSquare
      repo_name: peavers/PeaversAlwaysSquare
    secrets: inherit
